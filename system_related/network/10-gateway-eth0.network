# Permanent base configuration for eth0.
# Always up, always DHCP. Metric 100 makes eth0 the preferred ethernet uplink.
# This file is managed by the image layer — do NOT overwrite from scripts.
# Uplink policy (failback, priority, connectivity checks) is managed separately
# by gateway-network-apply and gateway-network-monitor.

[Match]
Name=eth0

[Link]
MTUBytes=1500

[Network]
DHCP=yes
MulticastDNS=no
LinkLocalAddressing=ipv6

[DHCPv4]
RouteMetric=100
UseDNS=yes
UseDomains=no
